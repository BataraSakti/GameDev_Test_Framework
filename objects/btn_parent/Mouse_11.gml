if(is_imploding == true) { exit; }
if(!can_click){exit;}
if(flat){ exit; }

sprite_index = sprite;

//TweenFireBounce(id, image_xscale__, true, EaseInOutQuad, p_xscale_start, p_xscale_start-0.1, 0.15);
//TweenFireBounce(id, image_yscale__, true, EaseInOutQuad, p_yscale_start, p_yscale_start-0.1, 0.15);

